meta {
  name: manage invitation
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/api/user/invitations/{{invitationID}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "isAccepted": true
  }
}
