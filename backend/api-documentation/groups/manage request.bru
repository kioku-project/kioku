meta {
  name: manage request
  type: http
  seq: 12
}

put {
  url: {{baseUrl}}/api/groups/{{groupID}}/members/requests/{{requestID}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "isAccepted": true
  }
}
