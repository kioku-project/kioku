meta {
  name: unsubscribe notifications
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/user/notifications/{{subscriptionID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
